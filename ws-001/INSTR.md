warp advertised a text editor thing/terminal thing that update when the underlying file changes, but has vim bindings, and allows you to co-edit while a LLM or something else is editing - investigate if that's true (first use web search to figure out what the actual claim is, then read this codebase to find out if it's implemented and whether it has any github issues reported).

maybe this phrase:
Take control of the code without leaving your flow. Open, edit, and save files in Warp's native file editor.
